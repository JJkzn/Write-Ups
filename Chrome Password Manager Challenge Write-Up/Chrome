Get Task Files
.pcapng file
Open In WireShark

SMB Data Exfiltration detected
Export SMB Objects:

File -> Export Objects -> SMB

The two large files, transfer.exe and encrypted_files are particularly interesting.

We git clone dnSpy to open the file

git clone --recursive https://github.com/dnSpy/dnSpy.git
cd dnSpy
# or dotnet build
./build.ps1 -NoMsbuild

